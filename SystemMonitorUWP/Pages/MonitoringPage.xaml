<Page
   x:Class="SystemMonitorUWP.Pages.MonitoringPage"
   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
   xmlns:local="using:SystemMonitorUWP.Pages"
   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
   mc:Ignorable="d"
   Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel Margin="20" HorizontalAlignment="Left">
            <TextBlock Text="Monitoring Status:" FontWeight="Bold" FontSize="24"></TextBlock>
            <TextBlock Text="{x:Bind MemoryLoad}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind TotalMemory}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind AvailableMemory}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind TotalPageFile}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind AvailablePageFile}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind TotalVirtualMemory}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind AvailableVirtualMemory}" FontSize="18"></TextBlock>
            <TextBlock Text="{x:Bind AvailableExtendedMemory}" FontSize="18"></TextBlock>
        </StackPanel>
   </Grid>
</Page>
